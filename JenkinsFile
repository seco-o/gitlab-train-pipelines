pipeline {
    agent any
    stages {
        stage('Build') {
        steps {
            echo "Runnning build automation"
            sh './gradlew npm_start'
            archiveArtifacts artifacts: 'dist/trainSchedule.zip'
        }

        }
    }
}

